/**
 * Year level model
 */
export interface Selectable {
  img_path: string;
  id: number;
  name: string;
}

/**
 * Year level model
 */
export interface CastMember extends Selectable {
  img_path: string;
  id: number;
  name: string;
}
