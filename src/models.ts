/**
 * Cast member model
 */
export interface CastMember {
  img_path: string;
  id: number;
  name: string;
}

/**
 * Movie model
 */
export interface Movie {
  img_path: string;
  id: number;
  title: string;
}

